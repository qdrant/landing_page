This code snippet sets up a field condition that matches keyword values starting with a given prefix. Here, a `prefix` match is defined with the target prefix `https://qdrant.`. Prefix matching is byte-wise and case-sensitive, consistent with exact keyword matching. It is served efficiently when the field has a keyword index created with the `prefix` option enabled; otherwise the condition falls back to a full scan.
